use std::{collections::{BTreeMap,BTreeSet},env,fs,path::{Path,PathBuf}};
use serde::{Deserialize,Serialize};
use serde_json::Value;
use crate::security::SecurityProfile;

#[derive(Debug,Clone,Deserialize,Serialize)]
pub struct Config{#[serde(default)]pub server:ServerConfig,#[serde(default)]pub auth:AuthConfig,#[serde(default)]pub security:SecurityConfig,#[serde(default)]pub approvals:ApprovalConfig,#[serde(default)]pub limits:LimitsConfig,#[serde(default)]pub lsp:LspConfig,#[serde(default)]pub workspaces:BTreeMap<String,WorkspaceConfig>,#[serde(default)]pub user_files:BTreeMap<String,UserFilesConfig>}
#[derive(Debug,Clone,Deserialize,Serialize)]pub struct ServerConfig{#[serde(default="default_listen")]pub listen:String}impl Default for ServerConfig{fn default()->Self{Self{listen:default_listen()}}}
#[derive(Debug,Clone,Deserialize,Serialize)]pub struct AuthConfig{#[serde(default="default_api_key_env")]pub api_key_env:String,#[serde(default)]pub principals:BTreeMap<String,PrincipalConfig>}impl Default for AuthConfig{fn default()->Self{Self{api_key_env:default_api_key_env(),principals:BTreeMap::new()}}}
#[derive(Debug,Clone,Deserialize,Serialize)]pub struct PrincipalConfig{pub api_key_env:String,#[serde(default)]pub roles:Vec<AuthRole>}
#[derive(Debug,Clone,Copy,PartialEq,Eq,PartialOrd,Ord,Deserialize,Serialize)]#[serde(rename_all="snake_case")]pub enum AuthRole{Admin,Developer,Reviewer,Operator}
#[derive(Debug,Clone)]pub struct LoadedPrincipal{pub name:String,pub key:String,pub roles:Vec<AuthRole>}
#[derive(Debug,Clone,Deserialize,Serialize)]pub struct SecurityConfig{#[serde(default)]pub profile:SecurityProfile,#[serde(default="default_command_allowlist")]pub default_command_allowlist:Vec<String>}impl Default for SecurityConfig{fn default()->Self{Self{profile:SecurityProfile::Default,default_command_allowlist:default_command_allowlist()}}}
#[derive(Debug,Clone,Default,Deserialize,Serialize)]pub struct ApprovalConfig{#[serde(default)]pub required_paths:Vec<String>}
#[derive(Debug,Clone,Deserialize,Serialize)]pub struct LimitsConfig{#[serde(default="default_max_body_bytes")]pub max_body_bytes:usize,#[serde(default="default_max_in_flight")]pub max_in_flight:usize,#[serde(default="default_command_timeout")]pub command_timeout_seconds:u64,#[serde(default="default_max_command_timeout")]pub max_command_timeout_seconds:u64,#[serde(default="default_command_output_bytes")]pub command_output_bytes:usize,#[serde(default="default_max_jobs")]pub max_jobs:usize,#[serde(default="default_job_retention_seconds")]pub job_retention_seconds:u64}impl Default for LimitsConfig{fn default()->Self{Self{max_body_bytes:default_max_body_bytes(),max_in_flight:default_max_in_flight(),command_timeout_seconds:default_command_timeout(),max_command_timeout_seconds:default_max_command_timeout(),command_output_bytes:default_command_output_bytes(),max_jobs:default_max_jobs(),job_retention_seconds:default_job_retention_seconds()}}}
#[derive(Debug,Clone,Default,Deserialize,Serialize)]pub struct LspConfig{#[serde(default)]pub servers:BTreeMap<String,LspServerConfig>}
#[derive(Debug,Clone,Deserialize,Serialize)]pub struct LspServerConfig{#[serde(default="default_true")]pub enabled:bool,pub argv:Vec<String>,#[serde(default)]pub extensions:Vec<String>,pub language_id:Option<String>,pub initialization_options:Option<Value>}
#[derive(Debug,Clone,Deserialize,Serialize)]pub struct WorkspaceConfig{pub root:PathBuf,#[serde(default)]pub capabilities:Capabilities}
#[derive(Debug,Clone,Default,Deserialize,Serialize)]pub struct Capabilities{#[serde(default)]pub fs_read:bool,#[serde(default)]pub fs_write:bool,#[serde(default)]pub commands:bool,#[serde(default)]pub git_read:bool,#[serde(default)]pub git_write:bool,#[serde(default)]pub git_network:bool}
#[derive(Debug,Clone,Deserialize,Serialize)]pub struct UserFilesConfig{pub root:PathBuf,#[serde(default)]pub read:bool,#[serde(default)]pub write:bool}

impl Config{
 pub fn load(path:&Path)->Result<Self,ConfigError>{let raw=fs::read_to_string(path).map_err(|source|ConfigError::Read{path:path.to_path_buf(),source})?;let config:Self=toml::from_str(&raw).map_err(ConfigError::Parse)?;config.validate()?;Ok(config)}
 pub fn auth_principals(&self)->Result<Vec<LoadedPrincipal>,ConfigError>{let mut loaded=Vec::with_capacity(self.auth.principals.len()+1);loaded.push(LoadedPrincipal{name:"admin".into(),key:read_secret(&self.auth.api_key_env)?,roles:vec![AuthRole::Admin]});for(name,p)in &self.auth.principals{loaded.push(LoadedPrincipal{name:name.clone(),key:read_secret(&p.api_key_env)?,roles:p.roles.clone()});}for l in 0..loaded.len(){for r in l+1..loaded.len(){if loaded[l].key==loaded[r].key{return Err(ConfigError::Invalid(format!("auth principals {:?} and {:?} resolve to the same API key",loaded[l].name,loaded[r].name)));}}}Ok(loaded)}
 fn validate(&self)->Result<(),ConfigError>{if self.server.listen.trim().is_empty(){return Err(ConfigError::Invalid("server.listen must not be empty".into()));}validate_env_name("auth.api_key_env",&self.auth.api_key_env)?;for(name,p)in &self.auth.principals{if name.trim().is_empty()||name.chars().any(char::is_whitespace){return Err(ConfigError::Invalid("auth principal names must be non-empty and whitespace-free".into()));}validate_env_name(&format!("auth.principals.{name}.api_key_env"),&p.api_key_env)?;if p.roles.is_empty(){return Err(ConfigError::Invalid(format!("auth principal {name:?} must have at least one role")));}let unique=p.roles.iter().copied().collect::<BTreeSet<_>>();if unique.len()!=p.roles.len(){return Err(ConfigError::Invalid(format!("auth principal {name:?} contains duplicate roles")));}}if self.security.default_command_allowlist.is_empty(){return Err(ConfigError::Invalid("security.default_command_allowlist must not be empty".into()));}for program in &self.security.default_command_allowlist{if program.trim().is_empty()||program.starts_with('-')||program.contains('/')||program.chars().any(char::is_whitespace){return Err(ConfigError::Invalid(format!("invalid default command allowlist entry {program:?}")));}}for path in &self.approvals.required_paths{if !path.starts_with("/v1/")||path.chars().any(char::is_whitespace){return Err(ConfigError::Invalid(format!("approval path {path:?} must be an exact whitespace-free /v1/... path")));}if path.starts_with("/v1/approvals/"){return Err(ConfigError::Invalid("approval management endpoints cannot require their own approval".into()));}}for(name,s)in &self.lsp.servers{if name.trim().is_empty(){return Err(ConfigError::Invalid("LSP server names must not be empty".into()));}if s.argv.is_empty()||s.argv[0].trim().is_empty(){return Err(ConfigError::Invalid(format!("LSP server {name:?} must define a non-empty argv")));}if s.argv.iter().any(|a|a.as_bytes().contains(&0)){return Err(ConfigError::Invalid(format!("LSP server {name:?} argv contains a NUL byte")));}if s.extensions.iter().any(|e|e.trim_matches('.').is_empty()||e.chars().any(char::is_whitespace)){return Err(ConfigError::Invalid(format!("LSP server {name:?} has an invalid extension mapping")));}}if self.limits.max_body_bytes<1024||self.limits.max_body_bytes>64*1024*1024{return Err(ConfigError::Invalid("limits.max_body_bytes must be between 1024 and 67108864".into()));}if self.limits.max_in_flight==0||self.limits.max_in_flight>1024{return Err(ConfigError::Invalid("limits.max_in_flight must be between 1 and 1024".into()));}if self.limits.command_timeout_seconds==0||self.limits.max_command_timeout_seconds==0||self.limits.command_timeout_seconds>self.limits.max_command_timeout_seconds||self.limits.max_command_timeout_seconds>3600{return Err(ConfigError::Invalid("command timeout limits are invalid or exceed one hour".into()));}if self.limits.command_output_bytes==0||self.limits.command_output_bytes>16*1024*1024{return Err(ConfigError::Invalid("limits.command_output_bytes must be between 1 and 16777216".into()));}if self.limits.max_jobs==0||self.limits.max_jobs>4096{return Err(ConfigError::Invalid("limits.max_jobs must be between 1 and 4096".into()));}validate_roots("workspace",self.workspaces.iter().map(|(n,c)|(n,&c.root)))?;validate_roots("user-files mount",self.user_files.iter().map(|(n,c)|(n,&c.root)))?;Ok(())}
}
fn read_secret(name:&str)->Result<String,ConfigError>{let value=env::var(name).map_err(|_|ConfigError::MissingSecret(name.to_owned()))?;if value.len()<32||value.trim()!=value||value.chars().any(char::is_control){return Err(ConfigError::Invalid(format!("API key from {name} must be at least 32 characters and contain no surrounding whitespace or control characters")));}Ok(value)}
fn validate_env_name(label:&str,value:&str)->Result<(),ConfigError>{if value.trim().is_empty()||value.chars().any(char::is_whitespace){Err(ConfigError::Invalid(format!("{label} must be a non-empty environment variable name")))}else{Ok(())}}
fn validate_roots<'a>(kind:&str,roots:impl Iterator<Item=(&'a String,&'a PathBuf)>)->Result<(),ConfigError>{for(name,root)in roots{if name.trim().is_empty(){return Err(ConfigError::Invalid(format!("{kind} names must not be empty")));}if !root.is_absolute(){return Err(ConfigError::Invalid(format!("{kind} {name:?} root must be an absolute path")));}}Ok(())}
fn default_listen()->String{"127.0.0.1:8787".into()}fn default_api_key_env()->String{"TUXBRIDGE_API_KEY".into()}fn default_max_body_bytes()->usize{10*1024*1024}fn default_max_in_flight()->usize{32}fn default_command_timeout()->u64{120}fn default_max_command_timeout()->u64{900}fn default_command_output_bytes()->usize{2*1024*1024}fn default_max_jobs()->usize{128}fn default_job_retention_seconds()->u64{3600}fn default_true()->bool{true}fn default_command_allowlist()->Vec<String>{["pwd","ls","cat","head","tail","wc","grep","stat","du","df","uname","id","whoami"].into_iter().map(str::to_owned).collect()}
#[derive(Debug)]pub enum ConfigError{Read{path:PathBuf,source:std::io::Error},Parse(toml::de::Error),MissingSecret(String),Invalid(String)}impl std::fmt::Display for ConfigError{fn fmt(&self,f:&mut std::fmt::Formatter<'_>)->std::fmt::Result{match self{Self::Read{path,source}=>write!(f,"failed to read {}: {source}",path.display()),Self::Parse(source)=>write!(f,"failed to parse configuration: {source}"),Self::MissingSecret(name)=>write!(f,"required API key environment variable {name} is missing"),Self::Invalid(message)=>write!(f,"invalid configuration: {message}")}}}impl std::error::Error for ConfigError{fn source(&self)->Option<&(dyn std::error::Error+'static)>{match self{Self::Read{source,..}=>Some(source),Self::Parse(source)=>Some(source),Self::MissingSecret(_)|Self::Invalid(_)=>None}}}
#[cfg(test)]mod tests{use super::*;use std::io::Write;#[test]fn rejects_relative_workspace_root(){let mut file=tempfile::NamedTempFile::new().unwrap();writeln!(file,"[workspaces.demo]\nroot = \"relative/path\"").unwrap();let err=Config::load(file.path()).unwrap_err();assert!(err.to_string().contains("absolute path"));}#[test]fn applies_defaults(){let mut file=tempfile::NamedTempFile::new().unwrap();writeln!(file,"[workspaces.demo]\nroot = \"/tmp\"").unwrap();let config=Config::load(file.path()).unwrap();assert_eq!(config.server.listen,"127.0.0.1:8787");assert_eq!(config.auth.api_key_env,"TUXBRIDGE_API_KEY");assert!(config.auth.principals.is_empty());assert_eq!(config.limits.max_in_flight,32);assert_eq!(config.security.profile,SecurityProfile::Default);assert!(config.lsp.servers.is_empty());assert!(config.approvals.required_paths.is_empty());}}
